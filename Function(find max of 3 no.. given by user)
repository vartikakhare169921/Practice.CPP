#include <iostream>
using namespace std;
int max(int a,int b,int c)
{
    if(a>b && b>c)
    return a;
    else if(b>c && b>a)
    return b;
    else if(c>a && c>b)
    return c;
}
int main() 
{
int  a,b,c;
cout<<"enter 3 nos. ";
cin>>a>>b>>c;
cout<<max(a,b,c);
    return 0;
}
