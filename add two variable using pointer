#include <iostream>
using namespace std;
int add(int *p,int *q)
{  
    int sum=*p+*q;
    int *s=&sum;
    return *s;
}
int main()
 {
     int a,b;
     cout<<"enter two nos ";
     cin>>a>>b;
     int *p=&a;
     int *q=&b;
  cout<<add(&a,&b);
    return 0;
}
