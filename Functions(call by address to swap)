#include <iostream>
using namespace std;
    void swap(int *a,int *b)
{
    int temp;
    temp=*b;
    *b=*a;
    *a=temp;
}
   int main()
   {
       int a,b;
       cin>>a>>b;
       swap(&a,&b);
      cout<<"value of a is "<<a<<" "<<"value of b is "<<b;
    return 0;
}
