#include <iostream>
#include <math.h>
using namespace std;
int main() 
{
int n,r,digit=0;
cout<<"enter a 3 digit no.";
cin>>n;
int num=n;
while(n!=0)
{
    r=n%10;
    n=n/10;
    digit=r*r*r+digit;
}
if(digit==num)
cout<<"armstrong";
else
cout<<"not";
    return 0;
}
